[project]
name = "career-compass-api"
version = "1.0.0"
description = "Career Compass backend — Neon Postgres result capture + Gemini AI proxy"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "asyncpg>=0.29",
    "google-genai>=0.3",
    "slowapi>=0.1.9",
    "python-dotenv>=1.0",
    "pydantic>=2.7",
]

# FastAPI Cloud auto-discovers the ASGI app at app/main.py (the `app` object).
