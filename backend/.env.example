# Copy to backend/.env for local development. NEVER commit the real .env.
# In production, set these in the FastAPI Cloud dashboard instead.

# Neon pooled connection string (Neon dashboard -> Connection Details -> Pooled)
DATABASE_URL=postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require

# Google Gemini API key (server-side only)
GEMINI_KEY=your_gemini_api_key

# Random secret guarding GET /api/results (generate with: openssl rand -hex 24)
ADMIN_TOKEN=change_me_to_a_long_random_string

# Shared API key the frontend must send as X-Api-Key on write/AI routes.
# Must match the GitHub Actions secret API_KEY. Leave empty to disable the check.
# Generate with: python -c "import secrets;print(secrets.token_urlsafe(24))"
SITE_KEY=change_me_to_match_frontend

# CORS: comma-separated origins allowed to call the API
ALLOWED_ORIGINS=https://akshaynikhare.github.io

# Optional hardening toggles
# ENABLE_DOCS=0        # 1 to expose /docs, /redoc, /openapi.json
# ENFORCE_ORIGIN=1     # 0 to disable the Origin allowlist on writes
# RESULTS_RATE_LIMIT=10/minute

# Optional overrides
GEMINI_MODEL=gemini-2.0-flash
AI_RATE_LIMIT=20/minute
